namespace ConsoleApp
{
    public static class MarshalConfig
    {
        // [System.Runtime.InteropServices.FieldOffsetAttribute(offset)]
        // offset: The offset in bytes from the beginning of the structure to the beginning of the field.
        // The byte is a unit of digital information that most commonly consists of eight bits, 8 bits which can represent 256 values (0 through 255).
        public const int ByteSize = 4;
    }
}
